class Tournament {
  String id;
  String name;
  int playerNumber;
  int playerAstNumber;
  int matchesNumber;

  Tournament(this.id, this.name, this.playerNumber, this.playerAstNumber,
      this.matchesNumber);
}
